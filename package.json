{
  "name": "bemaniajs",
  "version": "0.2.0",
  "description": "canvas-based rhythm game for the web",
  "main": "index.js",
  "devDependencies": {
    "@babel/cli": "^7.20.7",
    "@babel/core": "^7.20.12",
    "@babel/preset-env": "^7.20.2",
    "nodemon": "^2.0.20"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d dist",
    "build:watch": "nodemon --watch src --exec \"babel src -d dist\"",
    "dev": "cd frontend && npm run start"
  },
  "author": "",
  "license": "ISC"
}
